---

- name: "Remove existing crons"
  cron: name="{{item.name}}" state=absent
  file: cloudwatch-metrics
  user: root
  with_items: "{{ cron_specs }}"
  ignore_errors: True
  no_log: True

- name: "Setup crons"
  cron: name="{{item.name}}" minute="{{item.minute}}" hour="{{item.hour}}" job="{{item.job}}"
  file: cloudwatch-metrics
  user: root
  with_items: "{{ cron_specs }}"
  no_log: True
